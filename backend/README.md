# Backend workspace

Phase 01 creates the Python package, locked uv environment, FastAPI shell, migrations, and tests here. Later phases add domain features without crossing the boundaries in `docs/ARCHITECTURE.md`.
